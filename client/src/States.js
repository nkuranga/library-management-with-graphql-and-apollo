import { useState } from 'react'

function states() {
    const Counting = () => {
        const a = 100
    }
    return (
        <div>
            {a}
            <button onClick={Counting}>Increement</button>
        </div>
    )
}
export default states